services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: friday
    ports:
      - "5432:5432"
    volumes:
      - friday_pg_data:/var/lib/postgresql/data

volumes:
  friday_pg_data:
